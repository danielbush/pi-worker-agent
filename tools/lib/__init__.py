"""Library packages for this repository's tools.

`core/` holds the task-index subsystem (domain, application, infrastructure).
`show_managed_file/` holds the managed-file subsystem behind the
`.agents/skills/show-managed-file` entry point.
"""
